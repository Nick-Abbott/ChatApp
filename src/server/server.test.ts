import { default as Server } from './Server';

describe('Server', () => {
  describe('.bootstrap()', () => {
    it('Should create a new Server object', () => {
      // Server.bootstrap();
    });
  });
});
